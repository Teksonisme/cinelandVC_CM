{% extends "first_layer.html.twig" %}
{% block titre %}

	{{ parent() }}
{% endblock %}
{% block contenu %}
	{{ include('form.html.twig') }}
		<div id="titre_liste">{{titre_liste}}</div>
		<div id="list">
			{% for film in films %}
				<p>
					{{film}}
				</p>
			{% endfor %}
		</div>

	{% endblock %}
