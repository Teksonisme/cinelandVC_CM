{% extends "first_layer.html.twig" %}
{% block titre %}
	{{ parent() }}
{% endblock %}
{% block contenu %}
	<div class="list">
		{% for acteur in acteurs %}
			<p>
				<a href="{{ path('detail_acteur', 
                { 'id': acteur.id }) }}">
                {{acteur.nomPrenom}}</a>
			</p>
		{% endfor %}
	</div>
	<div class="form1">
		{{ form(formulaire) }}
		</div>
	{% endblock %}
